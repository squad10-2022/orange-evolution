import React from "react";
import Title from "../Title";

const ModulesBox = ({ title, evolution, summary }) => {
    return (
        <ModulesBoxContainer>
            
        </ModulesBoxContainer>

    );
}